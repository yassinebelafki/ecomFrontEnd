import { EvaluationPipe } from './evaluation.pipe';

describe('EvaluationPipe', () => {
  it('create an instance', () => {
    const pipe = new EvaluationPipe();
    expect(pipe).toBeTruthy();
  });
});
